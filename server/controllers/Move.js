export default function Move(currentPosition, mousePosition, canvasCoords) {
    console.log(currentPosition, mousePosition, canvasCoords)
    // let length = Math.sqrt((currentPosition.x - mousePosition.x) ** 2 + (currentPosition.y - mousePosition.y) ** 2)

    // let unitVector = {
    //     x: (currentPosition.x - mousePosition.x) / length,
    //     y: (currentPosition.y - mousePosition.y) / length,
    // }

    // console.log(unitVector)
}
